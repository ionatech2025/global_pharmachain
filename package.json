{
  "name": "pharmachain",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "bun": ">=1.2.0",
    "node": ">=20.9.0"
  },
  "overrides": {
    "@radix-ui/react-slot": "1.3.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:e2e": "playwright test",
    "db:generate": "bun run --filter @pharmachain/db build",
    "db:push": "bun run --filter @pharmachain/db db:push",
    "db:migrate": "bun run --filter @pharmachain/db db:migrate",
    "db:migrate:deploy": "bun run --filter @pharmachain/db db:migrate:deploy",
    "db:seed": "bun run --filter @pharmachain/db db:seed",
    "db:studio": "bun run --filter @pharmachain/db db:studio",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@playwright/test": "^1.61.1",
    "turbo": "^2.10.0",
    "typescript": "^5.9.3"
  }
}
